import { resourceUsage } from "process";

export default function LogoutButton () {
    return (
        <button className="uppercase font-black text-white text-base">
            Logout
        </button>
    )
}